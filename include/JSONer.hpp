#ifndef JSON_ER
#define JSON_ER

#include <string>
#include "Types.hpp"

class JSON
{
    public:
        static void exportData(int numberOfDataPoints, int numberOfkeys, std::string keyNames[], TestValue** keyValues[])
        {
            
        }
};

#endif